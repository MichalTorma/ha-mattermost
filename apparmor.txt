#include <tunables/global>

profile mattermost flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  capability,
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Access to options.json and other files within /data
  /data/** rw,

  # Start new shells
  /bin/bash ix,
  /usr/bin/env ix,
  /bin/echo ix,
  /bin/cat ix,
  /bin/ls ix,

  # Access to SSL certs
  /ssl/** r,

  # Mattermost specific
  /mattermost/** rw,
  /mattermost/server/bin/mattermost ix,
  /proc/sys/net/core/somaxconn r,
  
  # Network access
  network,
  
  # Process management
  /proc/*/stat r,
  /proc/*/status r,
  /proc/sys/kernel/pid_max r,
}
